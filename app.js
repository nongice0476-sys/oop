const DATA={'ชุด 1':window.OOP_SET_1,'ชุด 2':window.OOP_SET_2,'ชุด 3':window.OOP_SET_3};const L=['A','B','C','D'];let setName=localStorage.getItem('oop_set')||'ชุด 1',part='mcq',submitted=false,ans={},wa={};const $=s=>document.querySelector(s),key=x=>'oop_'+setName+'_'+x;
function load(){try{ans=JSON.parse(localStorage.getItem(key('a'))||'{}');wa=JSON.parse(localStorage.getItem(key('w'))||'{}')}catch{ans={};wa={}}submitted=localStorage.getItem(key('sub'))==='1'}function save(){localStorage.setItem(key('a'),JSON.stringify(ans));localStorage.setItem(key('w'),JSON.stringify(wa))}
function norm(t){return String(t||'').toLowerCase().replace(/<[^>]+>/g,' ').replace(/[^a-z0-9ก-๙().='_]+/gi,' ').replace(/\s+/g,' ').trim()}
const ALIAS={"instance attribute":["instance attribute","object property","property ของ object","แอตทริบิวต์ของ object"],"class attribute":["class attribute","attribute ของ class","แอตทริบิวต์ของคลาส"],"shadow":["shadow","บัง","ทับ"],"lookup":["lookup","หา attribute","ค้นหา attribute"],"same object":["same object","object เดียวกัน","อ็อบเจกต์เดียวกัน"],"reference":["reference","อ้างถึง","ตัวอ้างอิง"],"alias":["alias","ชื่ออีกชื่อ","ตัวแปรอีกชื่อ"],"method":["method","เมธอด"],"instance":["instance","object","อ็อบเจกต์"],"property":["property","attribute","คุณสมบัติ"],"modify":["modify","แก้","เปลี่ยนค่า"],"delete property":["delete property","del property","ลบ property"],"delete object":["delete object","del object","ลบ reference"],"two objects":["two objects","2 objects","สอง object","สองอ็อบเจกต์"]};
function hit(t,c){let n=norm(t),xs=ALIAS[c]||[c];return xs.some(x=>n.includes(norm(x)))}function grade(q,t){t=String(t||'').trim();if(!t)return{score:0,label:'ยังไม่ตอบ',m:[],miss:q.concepts};let m=q.concepts.filter(c=>hit(t,c)),miss=q.concepts.filter(c=>!m.includes(c));let cov=m.length/q.concepts.length,len=Math.min(1,norm(t).length/140),reason=/(เพราะ|ดังนั้น|จึง|ทำให้|because|therefore|เนื่องจาก|หมายความว่า)/i.test(t)?1:0,code=/class |def |del |\w+\./i.test(t)?1:0;let sc=Math.round(100*(.68*cov+.12*len+.10*reason+.10*code));if(norm(t).length<20)sc=Math.min(sc,45);return{score:sc,label:sc>=85?'ครบถ้วนมาก':sc>=65?'ถูกหลัก แต่ยังขาดบางส่วน':sc>=40?'ได้บางส่วน':'ยังไม่เพียงพอ',m,miss}}function badge(g){return`<div class="grade"><b>${g.label} — ${g.score}%</b>${g.m.length?`<div>พบ: ${g.m.join(' • ')}</div>`:''}${g.miss.length?`<div style="color:var(--muted)">ยังขาด: ${g.miss.join(' • ')}</div>`:''}<small style="color:var(--muted)">ตรวจแบบ concept-based + reasoning + code evidence</small></div>`}
function mc(q){return`<section class="card" id="q${q.id}"><div class="qhead"><b>ข้อ ${q.id}</b><span>${q.topic} · ${q.difficulty}</span></div><div class="prompt">${q.prompt}</div><div class="options">${q.options.map((o,i)=>`<label class="option ${submitted?(i===q.answer?'correct':ans[q.id]===i?'wrong':'')}"><input type="radio" name="q${q.id}" value="${i}" ${ans[q.id]===i?'checked':''} ${submitted?'disabled':''}><b>${L[i]}.</b><span>${o}</span></label>`).join('')}</div>${submitted?`<div class="solution"><b>เฉลย ${L[q.answer]}</b><br>${q.explanation}</div>`:''}</section>`}
function wr(q){let g=submitted?grade(q,wa[q.id]):null;return`<section class="card" id="q${q.id}"><div class="qhead"><b>ข้อ ${q.id}</b><span>${q.topic} · ${q.difficulty}</span></div><div class="written-question"><span class="label">โจทย์</span><div class="prompt">${q.prompt}</div></div><textarea data-id="${q.id}" ${submitted?'readonly':''} placeholder="เขียน code + อธิบายเหตุผลและ output...">${wa[q.id]||''}</textarea>${submitted?badge(g)+`<div class="solution"><b>แนวคำตอบ</b><br>${q.solution}</div>`:''}</section>`}
function render(){$('#title').textContent='Python OOP — '+setName;let qs=DATA[setName][part];$('#qs').innerHTML='<h2>'+(part==='mcq'?'PART A — Multiple Choice':'PART B — Written / Code Reasoning')+'</h2>'+qs.map(q=>part==='mcq'?mc(q):wr(q)).join('');bind();nav();prog()}function bind(){document.querySelectorAll('input[type=radio]').forEach(x=>x.onchange=()=>{ans[+x.name.slice(1)]=+x.value;save();prog();nav()});document.querySelectorAll('textarea').forEach(x=>x.oninput=()=>{wa[+x.dataset.id]=x.value;save();prog();nav()})}function nav(){let all=[...DATA[setName].mcq,...DATA[setName].written];$('#nav').innerHTML=all.map(q=>{let d=q.id<=20?ans[q.id]!==undefined:(wa[q.id]||'').trim();return`<button class="${d?'done':''}" onclick="go(${q.id})">${q.id}</button>`}).join('')}function go(id){part=id<=20?'mcq':'written';tabs();render();setTimeout(()=>$('#q'+id)?.scrollIntoView({behavior:'smooth'}),20)}function prog(){let a=DATA[setName].mcq.filter(q=>ans[q.id]!==undefined).length,w=DATA[setName].written.filter(q=>(wa[q.id]||'').trim()).length;$('#progTxt').textContent=`${a+w}/25 (MCQ ${a}/20 • Written ${w}/5)`;$('#bar').style.width=((a+w)/25*100)+'%'}function tabs(){document.querySelectorAll('.tab').forEach(b=>b.classList.toggle('active',b.dataset.part===part))}document.querySelectorAll('.tab').forEach(b=>b.onclick=()=>{part=b.dataset.part;tabs();render()});$('#setSel').value=setName;$('#setSel').onchange=e=>{setName=e.target.value;localStorage.setItem('oop_set',setName);part='mcq';load();tabs();render()};
$('#submit').onclick=()=>{if(!submitted){if(!confirm('ส่งข้อสอบและเปิดเฉลยหรือไม่?'))return;submitted=true;localStorage.setItem(key('sub'),'1');render()}let c=DATA[setName].mcq.reduce((s,q)=>s+(ans[q.id]===q.answer),0),gs=DATA[setName].written.map(q=>grade(q,wa[q.id])),w=Math.round(gs.reduce((a,g)=>a+g.score,0)/gs.length),m=Math.round(c/20*100);$('#score').innerHTML=`<h2>${setName}</h2><p><b>ปรนัย:</b> ${c}/20 (${m}%)</p><p><b>อัตนัย:</b> ${w}%</p><p><b>คะแนนฝึกเฉลี่ย:</b> ${Math.round((m+w)/2)}%</p>`;scoreDlg.showModal()};$('#reset').onclick=()=>{if(!confirm('ล้างคำตอบชุดนี้?'))return;['a','w','sub'].forEach(x=>localStorage.removeItem(key(x)));ans={};wa={};submitted=false;render()};$('#copy').onclick=async()=>{let t=DATA[setName].written.map(q=>`ข้อ ${q.id}: ${wa[q.id]||'(ยังไม่ตอบ)'}`).join('\n\n');try{await navigator.clipboard.writeText(t);alert('คัดลอกแล้ว')}catch(e){prompt('คัดลอก:',t)}};$('#note').onclick=()=>window.open('./short-note.html','_blank');window.addEventListener('load',()=>{load();render()});